syntax = "proto3";
package addevm.addevm;

import "gogoproto/gogo.proto";
import "addevm/addevm/params.proto";

option go_package = "add-evm/x/addevm/types";

// GenesisState defines the addevm module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
}
